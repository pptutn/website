import styled from "styled-components";

const StyledVideoItem = styled.div`
  display: flex;
  justify-content: center;
  margin: 1rem;
`;

const StyledCaption = styled.caption`
  margin: 3rem;
  max-width: 500px;
  font-size: 30px;
  text-transform: uppercase;
`;

export { StyledVideoItem, StyledCaption };
